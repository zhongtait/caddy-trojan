name: Build Caddy-Trojan


on: 
    workflow_dispatch:

jobs:
  build-caddy-trojan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [linux]
        arch: [amd64, arm64]
        go-version: [1.22.x]
    

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: true
          cache-dependency-path: |
            **/go.sum
    
      - name: Build Xcaddy
        run: go install github.com/caddyserver/xcaddy/cmd/xcaddy@latest
    
      - name: Build Caddy-Trojan
        run: |
          mkdir -p ~/output
          cd ~/output
          xcaddy build --with github.com/imgk/caddy-trojan
          echo "MY_CADDY_VERSION=$(./caddy version | cut -d ' ' -f 1)" >> $GITHUB_ENV
          cd ~
          tar -czvf caddy-trojan_${{ matrix.os }}_${{ matrix.arch }}.tar.gz ~/output
          echo $MY_CADDY_VERSION
