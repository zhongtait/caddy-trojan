# Keep repository text files executable and consistent across platforms.
* text=auto eol=lf

# Never apply line-ending conversion to binary archives or executables.
*.gz binary
*.zip binary
caddy binary

# Patch files intentionally contain diff-prefix whitespace and source tabs.
patches/*.patch whitespace=-blank-at-eol,-blank-at-eof,-space-before-tab
